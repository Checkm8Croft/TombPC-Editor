<Window x:Class="TombPC_Editor.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Parse Log" Height="600" Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="#1e1e1e">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox x:Name="LogTextBox"
                 Grid.Row="0"
                 Background="#1e1e1e"
                 Foreground="#d4d4d4"
                 FontFamily="Consolas"
                 FontSize="11"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap"
                 Margin="6" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="6">
            <Button Content="Copy All" Click="CopyAll_Click" Width="80" Margin="0,0,6,0" />
            <Button Content="Save" Click="Save_Click" Width="80" Margin="0,0,6,0" />
            <Button Content="Close" Click="Close_Click" Width="80" />
        </StackPanel>
    </Grid>
</Window>
